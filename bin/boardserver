#!/usr/bin/env python3
"""Board Server — Lightweight project board for AI agents."""

import os
import sys

script_path = os.path.realpath(__file__)
project_dir = os.path.dirname(os.path.dirname(script_path))
sys.path.insert(0, project_dir)

from boardserver.cli import main

main()
