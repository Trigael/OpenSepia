#!/usr/bin/env python3
"""OpenSepia — AI Dev Team CLI."""

import os
import sys

# Ensure we run from the project directory
project_dir = os.path.dirname(os.path.abspath(__file__))
os.chdir(project_dir)
sys.path.insert(0, project_dir)

from orchestrator.cli import main

main()
